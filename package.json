{
  "name": "sony-tv-remote-api",
  "version": "1.0.0",
  "description": "Integration API towards sony tv",
  "main": "index.js",
  "type": "module",
  "simple-git-hooks": {
    "pre-push": "./deploy.sh"
  },
  "scripts": {
    "build": "./node_modules/esbuild/bin/esbuild server.js --platform=node --bundle --outfile=dist/app.cjs",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "rm -rf .git/hooks/ && npx simple-git-hooks"
  },
  "author": "Jesper Thörnberg",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.20.0",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "esbuild": "^0.14.36",
    "simple-git-hooks": "^2.7.0"
  }
}